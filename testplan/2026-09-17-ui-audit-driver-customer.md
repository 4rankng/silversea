# Driver and customer portal UI audit — 17 September 2026

Environment: local frontend 7175 / API 3001. Fresh isolated browser sessions. No production actions. Existing direct permissions, transport lifecycle and financial source rules remain unchanged.

## Acceptance criteria and route inventory

| ID | Role / surface | Action | Expected / evidence required |
|---|---|---|---|
| UI-DC-01 | Driver: `/my-trips`, new/accepted/history tabs | Navigate each tab, use keyboard arrows, select a job, back to list, change month. | Correct visible state; no stale or hidden selected job; clear loading/error/empty state. Screenshot and DOM after interaction. |
| UI-DC-02 | Driver: `/my-trips/:id`, two-orders, POD | Open actual assigned job; inspect task, notes, contacts, invoice details, container/seal and expense forms; submit empty/invalid local fixture values, cancel, reopen. | Fields and actions have meaningful labels; invalid data remains editable; cancellation preserves saved data; no accidental completion or duplicate expense. Unit regression for defects and UI outcome evidence. |
| UI-DC-03 | Driver: earnings, payslips and penalties | Open each page, change period, select visible details and return. | Correct loading/empty/error feedback, readable amounts, period retained; no fabricated zero when unknown. |
| UI-DC-04 | Driver: notification list/bell and account/profile sheet | Open, use keyboard, close by button/Escape/backdrop, reopen; inspect profile and password form validation without changing credentials. | Focus enters and returns correctly, controls remain reachable; no hidden duplicate overlays or offscreen content. |
| UI-DC-05 | Customer: shipments, shipment detail | Search/clear/no-match/recover; use filters/date picker/sort/pagination; open actual accessible shipment and each visible detail/action. | Matching results and scope retained, useful empty state, no false delivery confirmation. Customer receipt/discrepancy are direct business facts, never approval stages. |
| UI-DC-06 | Customer: debit notes and statement | Change filters/period, open available document/detail/export, clear filters and recover. | Consistent controls and accessible field labels; errors shown without replacing data with false zero; totals and scope retain existing accounting semantics. |
| UI-DC-07 | Customer: navigation/account scope | Open mobile menu/account, keyboard traversal/Escape, navigate all three pages; switch permitted customer scope where available. | Mobile closed navigation is not keyboard-focusable; open navigation controls/focus are reachable; selected scope remains consistent. |
| UI-DC-08 | All listed driver/customer surfaces | Repeat at 360/390/820/1440px with long labels and values. | No page overflow, clipped money, unusable pickers, inconsistent filter heights or nested decorative cards; phone scrolls normally without visible vertical scrollbar. Capture screenshot and geometry per route/width; unit tests alone do not prove CSS. |
| UI-DC-09 | Query and mutation recovery | Hold or fail selected GET requests in the local browser, retry; preserve unsent notes/inputs. | Visible loading/error/explicit retry; no background heartbeat or automatic mutation replay. Record controlled failure separately from normal API evidence. |

Initial status was **NOT TESTED** before changes. Final executed cases, PASS results and explicit uncovered combinations are recorded in `plans/260917-ui-audit/reports/driver-customer.md`; the inventory alone is not evidence of complete flow coverage. Customer work stopped when scope narrowed; final checks cover DRIVER only.

## Confirmed defects and regression criteria before implementation

| ID | Reproduction | Required result | Automated / browser evidence |
|---|---|---|---|
| UI-DC-10 | At 390px, Tab through the closed customer menu: hidden links receive focus offscreen. Open menu: focus stays on trigger and escapes to covered content. | Closed mobile navigation is inert and hidden from assistive technology. Open menu moves focus inside, traps forward/reverse Tab, Escape/backdrop/close returns focus; desktop navigation remains usable. | Layout component regression; actual keyboard at 390/820/1440 and screenshots. |
| UI-DC-11 | Open driver earnings with ordinary motion enabled: animated base salary / road / production / paid figures disagree with static totals while counting. | All monetary values show the authoritative response immediately and remain consistent during page entry and period changes. Container transitions may remain; amounts must never animate through invented balances. | Component regression simulating the animation adapter; per-frame browser capture. |
| UI-DC-12 | Earnings summary succeeds but the deduction-history API fails. | Keep the authoritative earnings visible; show deduction-history error with explicit retry, never a false zero count or “no deductions” state. Retry affects the failed read only and performs no mutation. | Component failure/retry case and controlled browser GET failure/recovery. |
| UI-DC-13 | Local customer account returns a successful customer-scope response with no linked customers; pages currently claim no work/no debit and statement gives generic 409. | Show one clear “Tài khoản chưa được liên kết khách hàng” state with contact guidance and explicit retry. Do not mount scoped business pages or imply genuine empty work/zero debt. API failure remains a distinct retryable failure. | Scope/layout component test and real unlinked-account browser check; preserve linked Canon portal. |
| UI-DC-14 | Statement read fails, or a new date range is loading after a successful read; header displays zero/stale balance and stale export remains enabled. Export failure hides successfully loaded ledger. | Missing/failed reads show unknown balance, explicit retry, and disabled exports while loading/error. Export failure is inline and preserves the loaded statement; retry uses the applied period. All financial values and scope come from the successful response. | Statement component read/export failure regressions plus intercepted GET browser failure/recovery. |
| UI-DC-15 | Statement date fields render at 32px while action buttons are 40/44px, giving one filter group inconsistent geometry. | Date fields and filter actions share the standard 34px control height with a desktop pointer and 44px touch height on phone/tablet; labels align, no overflow at all four audit widths. | Real computed-geometry assertions and screenshots; CSS layout is not proven by unit tests. |
| UI-DC-16 | Driver detail/e-POD mobile app-body computes padding-bottom:0 because the full-bleed :has rule overrides the weaker mobile 70px reserve. At maximum scroll the final action extends under fixed bottom navigation. | Keep full-bleed horizontal layout while reserving bottom navigation clearance at phone/tablet widths. The entire final action is visible/clickable at max scroll; desktop gets no phantom mobile gap. | Browser screenshot and button-bottom versus nav-top geometry at 360/390/820/1440. Shared shell owner coordinates fix. |
| UI-DC-17 | Driver expense-list GET returns 503: error banner and “Chưa có chi phí phát sinh nào” appear together. | Do not declare the list empty when loading failed. Keep explicit retry and any previously loaded rows; recovery shows the actual returned entries. No mutation occurs from retrying the read. | Component rejection→recovery test and browser-intercepted read failure, real API retry. |
| UI-DC-18 | Type a dispute reason for customer debit A, cancel, then open debit B. A’s reason is silently prefilled for B. | Start each new decision with an empty reason and no previous decision error. Retain the same decision’s input only during a failed save/retry. Cancellation must not carry financial commentary onto another document. | Component two-document regression and browser controlled debit-list fixture. |
| UI-DC-19 | Debit dispute API fails while its dialog remains open. Error is rendered behind the modal; user sees no failure feedback. | Render the API error inside the active decision dialog, keep reason, allow explicit retry/cancel. Successful export notices remain outside the dialog. | Component rejected mutation; intercepted POST503 in browser with no backend financial mutation. |
| UI-DC-20 | Driver penalty page successfully loads an empty current period but renders deduction amount as “—” beside “0 vụ”. | Distinguish a known zero deduction (0 ₫) from a missing/failed salary period (— with error/retry). | Component zero/unknown regression and normal empty-period browser page. |
| UI-DC-21 | Driver penalty empty state renders its block SVG at the far left while the message is centered. | Center the empty-state and loading glyph on the same axis as its message at 360/390/820/1440px; keep the filter header alignment unchanged. | Actual screenshot and icon-center versus empty-state-center browser geometry; not a unit-only CSS claim. |

## Driver PRD reconciliation follow-up — before implementation

| ID | PRD / reproduction | Required result | Evidence |
|---|---|---|---|
| UI-DC-22 | `ManHinhLaiXe.md` §4 and online-only rules: a real file is selected on `/my-trips/7778/pod`; intercepted file POST503 clears its input and loses the selected file. | Retain each failed file in a full-width row below its category header/actions, without squeezing the heading, with filename and explicit retry/discard; retry only the failed file using the same prepared bytes. Previously saved files remain. No mutation replay on reconnect. Leaving with unsent files warns; read-only state disables retry. | Unit failure→retry without reselect, independent categories, discard/read-only and navigation warning; actual Chrome controlled503→normal local API recovery with POST count/readback. |
| UI-DC-23 | PRD §4: `usePodUpload` passes `new Date()` for gallery files, burning upload-time into pixels as capture-time. | Only a real camera shutter supplies capture-time metadata; gallery/file selection keeps unknown capture time unknown. Retries preserve the original capture instant and do not stamp again. Visible capture timestamps use `Asia/Ho_Chi_Minh`, independent of browser timezone. | Scanner callback and POD compression regressions; canvas timestamp unit regression; real browser gallery POST/image readback. Physical camera remains separate evidence. |
| UI-DC-24 | TC-LX-TIENDO-007..010/015/019/022 and AC-CP-LX-10: successful POD lifecycle was previously untested in this lane. | On isolated local job with an unreconciled cost, one saved category cannot complete; both saved/openable categories permit one explicit completion. Failed submit sends no completion, retains evidence; manual retry completes and moves to history. No finance/approval gate or duplicate expense is created. | Actual local browser upload, image viewer, controlled submit503 then normal API completion; API before/after and observed network order. |
| UI-DC-25 | Real Chrome native Back loses pending POD despite existing leave-guard unit PASS. BrowserRouter's earlier Window popstate listener unmounts the page before its late guard runs. | Register one application popstate dispatcher before BrowserRouter; the current page guard decides before router navigation. Cancel restores the exact history index and retains mounted draft; accept leaves once. Keep config-template compatibility. Saved evidence alone does not prompt; optional unsent evidence must be explicitly discarded before completion navigation. | Listener-order regression with router registered before page guard; actual native Back cancel/accept, header Back and successful completion browser checks. |
| UI-DC-26 | `ManHinhLaiXe.md` §3/acceptance8 requires source labeling for inferred milestones. Actual completion of QA trip6835 created PICKED_UP/LOADING_OR_RETURNING/DELIVERED with no source marker; shared recorder published them as separate driver reports at completion time. | Only new auto-completed milestones carry an explicit “suy ra từ hoàn thành” note/source using existing storage. Their customer-visible text describes inferred completion-time recording, not observed pickup/GPS times. Preserve existing real milestones and historical rows. No new table or approval gate. | Backend completion regression asserts existing ORDER_RECEIVED untouched, all new inferred events labeled and publications truthful; explicit progress remains real. Real local completion API readback before/after; legacy unlabeled rows are not retrospectively fabricated. |
