# Operations production polish — 2026-09-15

Scope: page-specific driver, OPS, CUS/shipment/dispatch and customer portal UI. Preserve all current uncommitted work, domain behavior, permissions, direct-recording and online-only rules. No backend, financial logic, shared primitives or global-style changes. Retain the existing Be Vietnam Pro/palette, compact 12px data and inputs / 11px secondary labels, with usable touch controls.

| Case | Reproduction / probe | Required behavior |
|---|---|---|
| OPP-001 | Load driver board, detail and secondary pages at 390, 768 and 1440; click tabs/disclosures and read long content. | Data fills available width, headings/labels stay coherent, no horizontal document overflow, clear empty/loading/error feedback. |
| OPP-002 | Load OPS orders/wallet/fleet at three widths; open expense/advance dialogs and close by visible controls and Escape where supported. | Compact readable rows and controls, no nested-card excess, usable dialog body/footer on phones, truthful direct-action states. |
| OPP-003 | Load CUS overview/container pages and dispatch plan/detail; exercise search/filter disclosure and open an actual shipment detail. | Efficient dense layout, visible applied filters/actions, no cut-off labels or excessive mobile edge padding, stable error/empty states. |
| OPP-004 | Load customer shipment/list/detail/debit-note/statement pages at three widths; exercise navigation/filter/details. | Professional density and hierarchy, clear no-data/error feedback, useful mobile layouts without inaccessible tables or large blank bands. |

Before each implementation, record concrete discovered issue and acceptance probe below. Browser evidence must distinguish actual clicks/screenshots from source or automated component checks. Pure layout changes have no database mutation; state that explicitly in the coverage table. Parent owns combined lint/typecheck/full frontend/build; this lane runs scoped checks and actual Chrome interactions.

## Discovered issues / implementation probes

- OPP-002a — Actual Chrome OPS wallet at390x844: summary occupies292px; all three tables force880px width inside374px canvas, hiding reason/status/actions. Replace summary with one divided band and two-up supporting metrics; render labelled two-column records below700px container width. Verify full reasons/statuses readable at390/768/1440, no document overflow, unchanged amount/status values.
- OPP-002b — Source audit: wallet/fleet queries silently show empty data when failing; request section disappears on first load; settlement detail opens only after data arrives, so click has no immediate feedback and errors are invisible. Add bounded inline loading/error+retry; open settlement dialog immediately with loading/error body and persistent close control. Verify failed query never claims empty, retry recovers, details close while loading, and no financial mutations during layout-only browser probes.
- OPP-002c — Source audit: fleet forces820px on narrow screens, hiding driver/state/update. Use labelled, divider-separated two-column records when table container <700px, preserve desktop columns and full long text.
- OPP-002d — OPS orders failure text asks users to retry but has no retry control and simultaneously renders empty-list copy. Use the OPS query feedback pattern and suppress empty content on error. Test all four OPS read failures (summary/request/history/settlement), list retry, and detail loading/error dismissal.
- OPP-001a — Actual Chrome driver income390: decorative hero120px + four-row summary211px before actual ledgers; desktop summary also nests four cards inside a card. Flatten metric treatment and use two-column phone summary; preserve all amounts, ledger descriptions and data labels. Reduce detail fact icon chrome and label tracking without changing field order or disclosure touch targets.
- OPP-004a — Source audit: customer shipment detail uses the two-column summary header without its title wrapper, placing Bill/Book beside the eyebrow instead of a consistent title block. Its failed query has no retry and loading removes navigation. Restore consistent title hierarchy, keep Back in loading/error, retry failed detail; pair short facts on mobile while long addresses span both columns.
- OPP-001b — Actual Chrome driver income768: long explanatory note is right-aligned in a narrow numeric-value column, splits its label, and stretches the neighboring ledger into a blank panel. Render prose as a full-width left-aligned note; let parallel ledgers size to their content.
- OPP-002e — Full lint surfaced computed but unused expense amount validation. Negative input disables Save yet displays a positive formatted value with no reason. Preserve the entered minus sign, wire aria-invalid/describedby plus visible field error; verify no create call for invalid amount. Other parent-assigned lint corrections remove imports/local bindings only after confirming no remaining use.
- OPP-002f — Independent review: expense creation can save before receipt upload resolves, dropping the in-flight attachment. Disable Save and guard form submission while uploading; verify a deferred upload blocks both button and form submission, then the resolved storage key is included exactly once in creation.
- OPP-002g — Independent review: the receipt shortcut opens the editor for unlinked VOIDED/REJECTED records and exposes photo deletion for linked records. Use the same editable predicate for edit/delete/receipt actions; terminal or settled records open only the read-only photo viewer. Verify retained images can be viewed with no mutation controls.
