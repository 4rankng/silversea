# Production-branch role UI audit

Initial audit base: `a980c53c`; final pulled prod base: `cfa0753fa85764e829e60bb8fdf5993482885f33`. Local frontend `localhost:7175`, API `localhost:3001`.
All browser runs use canonical local test accounts. No production writes.

| Case | Reproduction | Expected |
|---|---|---|
| SIS-ROLE-01 | As CUS open `/config/customers`, type a customer search, clear it and open a customer details disclosure. The current page requests the forbidden `/api/trips` list and silently renders a missing-revenue KPI. | The catalog remains usable without requesting trip statistics for CUS. Explain unavailable revenue access. The list, search, details and permitted customer actions remain available. No permission errors. |
| SIS-ROLE-02 | As MANAGER/DISPATCHER open `/config/customers`, type and clear several searches. The current query re-fetches every trip page for each changed search. Delay/fail the trip-statistics read, then retry. | Load optional current-month trip statistics independently of customer search. Search does not re-fetch trip statistics or wait for them; summaries and exports retain available statistics. A statistics read failure does not hide the customer catalog and is distinguishable from a real zero. For roles with trip-statistics access, export stays disabled while statistics are pending or failed, then enables when they load; CUS catalog export stays available without forbidden trip reads. |
| SIS-ROLE-03 | Visit operational CUS, DISPATCHER, DRIVER, OPS, CUSTOMER, ACCOUNTANT and MANAGER routes at390/820/1440px. Open representative filters, forms, detail panels and tabs without submitting financial commands. | Routes permitted for the role load with no crash or document overflow. Dialogs remain within viewport, actual controls respond, Escape dismisses overlays and returns focus. Coverage is recorded per route/control; an inventory screenshot alone is not interaction coverage. |
| SIS-ROLE-04 | As ADMIN visit configuration routes, open each distinct available create/edit control at390/1440px, capture the resulting form, then dismiss unchanged dialogs with Escape. | Responsive forms fit the viewport, titles/fields/actions remain visible, unchanged dialogs dismiss and return focus to the trigger. Route-based editors are recorded separately. No save, deletion, import or configuration mutation is submitted. |
| SIS-ROLE-05 | At `/config/routes` open `Thêm tuyến` then Escape at390/1440px. The dynamic form key currently unmounts the open modal, bypassing its normal focus restoration. Also type an unsaved draft, cancel, and reopen. | Escape returns keyboard focus to `Thêm tuyến`. Reopened create form starts empty; edit forms load their selected route. Keep the existing form reset effect and normal overlay lifecycle, without saving a route. |
| SIS-ROLE-06 | At `/suppliers` inspect each row action at1440px and open the first menu, then create/cancel/reopen at390/1440px. Desktop row action icons currently have no accessible name or expanded state; pressing Enter on the menu instead navigates to the supplier detail because the row key handler consumes nested controls. The create modal dynamic key also reproduces lost focus on both widths. | Every row menu has a supplier-specific accessible name and correct expanded state; clicking or keyboard-activating it keeps the user on the supplier list. Direct row Enter/Space navigation remains available. Create dismissal returns focus and abandoned form drafts reset on reopen after removing the redundant dynamic modal key. No supplier write. |
| SIS-ROLE-07 | Latest prod real browser at390/1440: a source appointment copies into two empty rows while preserving another complete appointment, but silently overwrites a partial `14:` destination and counts it as empty. Keyboard Tab skips the hidden Copy button. Recheck both partial time and partial date, existing complete destination, count and keyboard/mobile access. | Snapshot eligible target keys at click, using existing date-draft detection outside the state updater; copy only truly empty destinations and preserve complete/partial drafts with the actual copied count. Preserve source eligibility behavior while making Copy reachable through focus-within and visible on compact/coarse-pointer surfaces, with44px target below inputs. No shipment is saved. |
| SIS-ROLE-08 | As CUS open customer create/edit. CustomerForm unconditionally requests `/api/debit-note-templates` and receives403. DISPATCHER can read templates but the backend strips billing-template edits for both intake roles. | Only ADMIN/MANAGER/ACCOUNTANT request and choose templates, matching existing server edit capability. Intake forms explain that accounting manages templates; existing template IDs remain unchanged in the form draft. CUS create opens without403; a locally intercepted Save503 keeps the form, draft and session, then cancel without a database write. |

Evidence: `qa/2026-09-17-sisprod/roles/`; final coverage and limitations: `plans/260917-sisprod/reports/role-browser.md`.
