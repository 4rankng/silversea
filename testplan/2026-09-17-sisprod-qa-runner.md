# SISPROD local QA runner regression

Status: harness contracts, eight-role navigation smoke, full serial E2E and affected-suite reruns executed. Latest per-suite evidence: 324 PASS, 0 FAIL, 1 SKIP across 19 suites; the full gate remains incomplete because suite16 needs an external workbook. Scope: test harness reporting, effective-target checks, assertions and fixture isolation; no application API or business behavior changes. Root owns production-source browser QA and patch packaging.

## Cases

- **SIS-QA-001 — Smoke rejects broken data at a correct URL.** Given a correct role-home URL, when the role API returns non-2xx, a page exception/console error is captured, an API request fails, the page stays in a loader, or no main content renders, the smoke result must be non-passing and retain the reason. A healthy run with all required evidence passes.
- **SIS-QA-002 — Root redirect is actually checked.** A wrong root landing or a URL merely containing the expected path must fail. Direct home and role-blocked routes must retain their current assertions. Admin's expected root home is `/config`.
- **SIS-QA-003 — Every suite file is selected.** Default discovery includes all numeric `test_NN_*.py` files, including both suite-20 scripts. Selecting `20` runs both. Unknown/invalid selectors fail explicitly; missing number gaps do not create phantom suites.
- **SIS-QA-004 — Missing/incomplete evidence cannot pass.** Empty runs, missing JSON results, non-PASS rows, and inconsistent totals are non-passing. SKIP/BLOCKED remain labeled as such in output; no skipped case is relabeled PASS. A complete all-PASS artifact succeeds.
- **SIS-QA-005 — Script exit follows suite result.** The suite-20 visual script propagates the existing run_suite return code. Shared Python summary cannot report an empty or skipped-only suite as successful.
- **SIS-QA-006 — Driver visual checks retain observed failures.** TC-3002/3003/3011 must not pass when the required navigation count, labels or active tab are absent. TC-3031/3033/3035/3036 must report missing sections or an unidentified/non-sticky acceptance control. TC-3034 follows the current PRD's removal of duplicate vehicle rows. TC-3040 must fail when the clicked order is absent from the received board; unrelated existing cards are not evidence. TC-3060 requires a visible history card or the real rendered empty-history message; missing fixtures or loading content must not be called a pass.
- **SIS-QA-007 — Direct login waits for its actual outcome.** Submit the existing MANAGER, DRIVER and OPS credentials through the UI. Wait at most 15 seconds for the exact role-home path; preserve the original expected paths. A pending request after an arbitrary one-second sleep is not a finished login outcome. A timeout or wrong route must remain FAIL and include the rendered login error when present. The first full run screenshot shows the login action still pending at the failed assertion.
- **SIS-QA-008 — Driver workflow assertions follow the current contract.** The current driver PRD requires four primary navigation actions and no duplicate đầu kéo/moóc rows in order detail (`docs/prd/ManHinhLaiXe.md`, sections 2 and 3). The isolated FCL fixture must have the expected single-trip tag, route/contact sections, and the identified sticky acceptance control. Missing required content cannot pass under a different-label explanation. Acceptance must target the sticky action and verify the same fixture trip in the received list; a disabled control retains its real guard reason.
- **SIS-QA-009 — Driver visual scale and viewport match the approved contract.** `frontend/docs/design-system.md` sets body/data/controls at 12px and captions at 11px; required card route/action text must meet the body scale. A closed off-canvas sidebar must not intersect the mobile viewport; layout visibility alone does not establish that it is on screen. The visual suite uses the same grounded checks already present in the workflow suite.
- **SIS-QA-010 — Credential rejection cannot hide a service failure.** TC-0011 submits an incorrect password and must receive HTTP 401. A 403/429/5xx or transport failure must fail the credential assertion rather than pass as a generic auth error.
- **SIS-QA-011 — Known legacy fallback passes assert actual controls/data.** TC-0107 searches using the API-created trip code (the UI's displayed identity, across periods) and requires the matching trip-detail link and rendered code, rather than an unrelated old customer reference. TC-1143 requires the actual accessible `Thời gian` combobox; the adapter's hidden native select must not hide a missing visible control. Missing prerequisite trip identity is SKIP; missing required rendered content/control is FAIL. No alternative-label fallback is a pass.
- **SIS-QA-012 — Legacy visual fixture failures are diagnosed.** The primary navigation is found by its accessible name, as in the successful isolated workflow suite. The initial run selected an accounting-locked historical seed with no factory, causing an invalid positive-fixture assumption and disabled-button timeout. The final suite uses its own eligible fixture (SIS-QA-014); missing factory or disabled acceptance for that fixture remains FAIL with the rendered guard reason, rather than skipping the positive assertions.
- **SIS-QA-013 — E2E effective targets are validated before workflows.** Default endpoints remain local. Explicit remote staging frontend/API pairs with the same scheme and host remain supported; both must be supplied. Mixed local/remote targets, mismatched remote hosts/schemes, unpaired remote overrides and invalid URLs fail before suites. Connectivity checks must use the actual effective endpoint hosts/ports, and print the effective targets. Pure tests never connect remotely.
- **SIS-QA-014 — Driver visual QA owns its acceptance fixture.** Reuse the successful native CUS submission/direct-dispatch setup from suite20 flow to create one populated, unlocked order owned by the demo driver. Select its exact trip-code card for all visual/detail/acceptance checks and clean up that exact trip in a `finally` block. Existing arbitrary locked/missing-factory seeds no longer determine whether the positive visual assertions execute. Setup/acceptance failures remain non-passing.
- **SIS-QA-015 — Responsive CUS assertions wait for the owned row.** At every viewport, await the exact newly created shipment row becoming visible before measuring overflow and sampling fixture content. Preserve the original overflow, content, drawer and focus assertions. A missing row after the bounded wait must still fail. The first final-source run sampled `visible_fixture=False` after a fixed300ms even though the following exact-row focus/drawer action passed and the retained desktop screenshot showed that same fixture. Retain that baseline and rerun the complete suite18 after replacing the fixed delay with this readiness condition.

## Validation

Run focused Node regression tests and Python unittest contract tests, shell/Node syntax checks, then current local smoke against real role pages. Record full commands/exit/output under `qa/2026-09-17-sisprod/route-inventory/`. Negative result-contract tests establish harness reporting only; actual E2E evidence is recorded separately. See `plans/260917-sisprod/reports/e2e-harness-results.md` for original failures, diagnoses and rerun provenance.

## Executed evidence

- `qa/2026-09-17-sisprod/route-inventory/node-contract-tests.log`: 33/33 Node contract cases passed. Initial Admin-home regression failure is preserved in `smoke-contract-before.log`; fixed before the passing run.
- `python-contract-tests.log`: initial 6/6 discovery/result cases passed. `python-contract-final.log`: final 11/11 discovery/result/target cases passed; remote-target cases are pure and make no remote connections.
- `all-suite-discovery.log`: 19 discovered script files; `driver-suite-discovery.log`: both suite-20 scripts. `invalid-suite.log`: unknown suite exits nonzero.
- `missing-workbook.log` + `missing-workbook/16-dispatch-workflow_results.json`: real missing local workbook stays SKIP and returns exit1, with no browser/DB workflow.
- `smoke-ui-driver.log` + `smoke/summary.json` and per-role screenshots: all eight roles passed current local navigation/root/guard/API200 checks at 1440px; no runtime or page API errors captured. This is navigation smoke, not full workflow-control coverage.
- `e2e-full.log`: all19 scripts executed serially; original298 PASS/7 FAIL/1 SKIP retained. Login pending-request timing and legacy visual selector/fixture failures were diagnosed and corrected.
- `e2e-rerun.log` and `e2e-rerun-summary.log`: 00/01/11/both20 scripts passed131/131 cases on the restarted API. This includes actual trip-code search, visible penalty filter, exact-trip acceptance and visual assertions using an owned fixture. Both driver fixtures were released successfully.
- `target-local.log`, `target-mixed-rejected.log`, `runner-gate.log`: effective local targets passed; mixed local/remote pair rejected before suites; final runner executed14 successfully and retained16 as SKIP with nonzero aggregate exit.
- Shell/Node/Python syntax and `git diff --check` passed for the harness changes. The separate `e2e/visual/run_section.py` framework received a result-contract change; its full section matrix was not rerun and is not implied by these E2E results.
- `final-source/spa-smoke-corrected.log`, `final-source/smoke-corrected/summary.json`, and `latest-spa-smoke-evidence.json`: frozen-source eight-role navigation/API smoke8/8 PASS, each API200 and no captured browser/API errors, exit0. Initial wrong-base-path invocation and owned-Chrome teardown cleanup are retained in `final-source/spa-smoke.log` and `final-source/smoke-teardown-note.txt`; assertions were unchanged.
- `final-source/e2e-18-19.log`: final API/CUS delta rerun90 PASS/1 FAIL/0 SKIP; suite19 passed54/54. The retained desktop failure and screenshot establish SIS-QA-015's pre-readiness sample. After the bounded exact-row visibility fix, `final-source/e2e-18-readiness.log` records complete suite18 at37/37 PASS, exit0. `final-source/suite18-readiness-checks.log` records Python AST/diff checks. These final runs used HEAD `cfa0753fa85764e829e60bb8fdf5993482885f33` plus frozen product fixes and API PID22549; no product code changed.
- `final-source/python-contract-final.log`: post-suite18-edit Python AST parsing and all11 contract tests passed, exit0. `final-source/final-owned-diff-check.log`: final scoped whitespace check passed for all21 owned files and the results report.
