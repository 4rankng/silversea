VITE_API_BASE=/api
# Driver-app spec (260827): "Nhập chi phí lô hàng" shipment-cost-entry form.
# Backend-ready but hidden from real drivers until rollout is approved.
# OFF by default — only "true"/"1"/"yes" enable it.
VITE_FEATURE_SHIPMENT_COST_ENTRY=false
