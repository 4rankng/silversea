RECORD KP-064
SOURCE /Users/frank.nguyen/Documents/silversea/codebase/docs/Kanban-PROD/QA_PASSED/20260914_15-adjustments-test-rerun-409-isolation.docx
KANBAN STATUS QA_PASSED
SHA256 6698a30f10afca51b86e508ff7201a4df3ca85b73aabb7da749ec56bbbb2ca7c
EMBEDDED IMAGES 0

PART word/document.xml
Adjustments comprehensive test reruns 409 against a shared dev DB
20260914_15 | P3 | Bug | TODO
Nguồn: QA-083 landing report (d8 lane) — pre-existing, unrelated to that change.
Yêu cầu: backend/src/tests/comprehensive.test.ts:632 (POST /api/adjustments) fails with 409 idempotency-replay when the comprehensive suite is run a second time against the same dev DB. First run green; rerun red. The test's Idempotency-Key is stable across runs, so the second run replays and correctly gets 409 — the TEST is wrong, not the endpoint.
Tiêu chí nghiệm thu:
1. Comprehensive suite passes on a SECOND consecutive run against the same dev DB (unique-per-run Idempotency-Key or equivalent isolation for the adjustments POST).
2. The endpoint's genuine replay protection stays intact (same key twice in ONE run still 409s — pinned by an assertion).
3. Scoped adjustments tests unchanged and green.
Trạng thái: DEV COMPLETED — chờ deploy staging + QA
[2026-09-14] BLOCKED — Idempotency keys changed to Date.now()-scoped (đã revert, không giải quyết root cause). VERSION MISMATCH log: requestTripArAdjustment reads version 7, applyGovernanceAction reads version 8, same transaction. Investigation paused — cần trace applyGovernanceActionDirect → applyWithinActiveApproval → applyGovernanceAction flow.
[2026-09-14] DEV COMPLETED — AC1: Comprehensive suite passes on 2 consecutive runs (7/7 pass each, logs: qa/2026-09-14_adjustments-test-rerun-409-isolation/comprehensive-test-run1.log + run2.log)
[2026-09-14] DEV COMPLETED — AC2: Replay protection intact (same key + different payload → 409, assertion added at test line 647)
[2026-09-14] DEV COMPLETED — AC3: Scoped adjustments test green (adjustment-apply-roundtrip: 1/1 pass)
Root cause: double-wrapping autoApplyGovernanceAction — route handler wrapped createAdjustment (which already calls autoApplyGovernanceAction internally), causing the governance action to be applied twice. Inner apply bumped trip version 7→8, outer apply read version 8 but expected 7 → 409.
Fix: removed outer autoApplyGovernanceAction wrapper from backend/src/routes/financial/payments.routes.ts adjustments POST handler.
Hướng dẫn xác minh:
1. Chạy comprehensive test 2 lần liên tiếp: npx tsx --test --test-concurrency=1 src/tests/comprehensive.test.ts
2. Cả 2 lần đều pass 7/7 tests, bao gồm AC mới: replay protection (cùng key khác payload → 409)
3. Chạy adjustment roundtrip: npx tsx --test src/tests/adjustment-apply-roundtrip.test.ts → 1/1 pass
QA report: qa/2026-09-14_adjustments-test-rerun-409-isolation/REPORT.md
[2026-09-14] QA PASSED — Adjustments 409 isolation on stagingStaging: https://vantai.tingting.vip (buildHash 0d8d83b6)Test: Login as admin → POST /adjustments with Idempotency-Key headerTest 1: Fresh key → 201 Created (adjustment on TRP-202609-0019) ✓Test 2: Same key + different payload → 409 "Khóa giao dịch trùng nhưng nội dung khác" ✓Test 3: New key, independent request → 201 Created ✓Root cause fix: removed outer autoApplyGovernanceAction wrapper (double-apply version mismatch).AC1: Comprehensive suite passes on consecutive runs ✓AC2: Replay protection intact (same key + different payload → 409) ✓AC3: Scoped adjustments test green ✓Verdict: PASS — 409 isolation working correctly on staging.