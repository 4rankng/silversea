RECORD KP-023
SOURCE /Users/frank.nguyen/Documents/silversea/codebase/docs/Kanban-PROD/QA_PASSED/20260913_2-detail-plan-tests-accumulation-trap.docx
KANBAN STATUS QA_PASSED
SHA256 9220ef3da730709311aec36e99d146e19ad2a83ed52c9df1bf221d10125fa18d
EMBEDDED IMAGES 0

PART word/document.xml
Nợ kỹ thuật: test detail-plan phụ thuộc DB tích luỹ (page-1 flaky)
Test 'zone filter narrows rows to a zone's ports (either side)' trong backend/src/tests/dispatch-detail-plan.test.ts đã hỏng trên DB dev tích luỹ: các fetch zone KHÔNG có q nên phân trang page-1 (limit 50) trên TOÀN BỘ row khớp zone trong DB, và lot của test rơi ra ngoài trang 1 khi DB đã nhiều dữ liệu seed.Đã fix 2026-09-13: thêm q=<suffix> vào 2 fetch zone (commit treo trong working tree, cùng q23 fix). Nợ còn lại: rà soát các test cùng lớp — mọi assertion 'membership trong page-1 không scoped' trên dispatch-detail-plan-rows/dispatch-master-plan đều có thể flaky theo mức tích luỹ của DB local; cân nhắc q-narrow hoặc cơ chế isolation (schema riêng / truncate bảng liên quan trong before()).Nguồn: kanban-work loop 2026-09-13 (backend focus), qa run iteration 3.
Trạng thái: QA PASSED — chờ deploy prod
[2026-09-13] QA PASSED — audit toàn bộ test chạm dispatch-detail-plan-rows/-facets:
1. dispatch-detail-plan.test.ts: 5 assert membership không scoped — zone ×2 (đã fix iteration trước) + 3 facet (delivery-point, pickup-port, dropoff-port) fix lần này bằng q=<suffix> (facet limit 100 xếp theo tên — service :507/:545 — nên membership không scoped sẽ rơi khi DB tích luỹ). File 71/71 pass.
2. dispatch-fulfillment.test.ts: 2 fetch đều đã q-scoped (:1579/:1658) — không cần đổi.
3. material-write-registry-exhaustive.test.ts: chỉ map metadata route, không fetch — không cần đổi.
4. Endpoint master-plan: không có test nào assert membership không scoped.
Gates: backend tsc 0 lỗi; full suite 186 unit + 1846 integration = 2032/2032 pass (EXIT=0, /tmp/backend-suite-260913-0320.log). Bằng chứng: qa/2026-09-13_detail-plan-tests-accumulation/REPORT.md