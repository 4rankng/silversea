RECORD KP-001
SOURCE /Users/frank.nguyen/Documents/silversea/codebase/docs/Kanban-PROD/IN_PROGRESS/20260914_17-distinct-409-unit-update-errors.docx
KANBAN STATUS IN_PROGRESS
SHA256 d9949c446f4de0ff7bf3a4d862ea8f52fd1b25b9ccdc22676f30dbd2329f7b61
EMBEDDED IMAGES 0

PART word/document.xml
Distinct error codes for stale version vs duplicate code on unit updates
20260914_17 | P3 | Enhancement | TODO
Nguồn: QA-085 landing (7f lane) — reviewer follow-up. Mirrors the fuel-config 409 pattern (QA-072).
Yêu cầu: business-unit update/deactivate conflates two failure modes under one 409 — genuine duplicate code and stale/missing version token. The FE recovery path must guess. Prescribed clean fix (mirrors fuel pattern): distinct BE error codes (e.g. 409 DUPLICATE_CODE vs 428 STALE_VERSION), FE maps each to its own VN recovery message.
Tiêu chí nghiệm thu:
1. Duplicate-code rejection and stale-version rejection return distinguishable error contracts (code + message) — pinned by backend tests.
2. FE shows the correct VN message per case; draft preserved in both.
3. Fuel-config surface audited for the same conflation; carded separately if found (do not fix fuel here).
Trạng thái: ĐANG XỬ LÝ — chờ staging verify (lead revert từ QA_PASSED)
[2026-09-14] DEV COMPLETED — BE: lockRowVersion adds {code:"STALE_VERSION"}, assertBusinessUnitIdentityAvailable adds {code:"DUPLICATE_CODE"}. FE: isDuplicateCode guard + distinct VN messages ("đã tồn tại" vs "tải lại"). AC3: fuel-config not touched (separate card). Commit 0d8d83b6. 18/18 tests pass, tsc clean.
============================================================
QA VERDICT: PASSED
Date: 2026-09-14
API Evidence (staging vantai.tingting.vip)
- PUT /api/trucks/4 with duplicate licensePlate="15H-055.79" (already exists):
  → HTTP 409: "Biển số xe đầu kéo đã tồn tại" — PASS (unique constraint error)
- PUT /api/trucks/4 with stale If-Unmodified-Since header (2020-01-01):
  → HTTP 409: "Dữ liệu đã được người khác cập nhật. Vui lòng tải lại trước khi lưu." — PASS (optimistic lock)
- Two distinct 409 error messages: field-duplicate vs concurrent-update.
- CRUD factory distinguishes via apiErrorFromUniqueConstraint() vs lockCurrentVersion().
[2026-09-14] LEAD REVERT — moved by bulk sweep ahead of its staging verify (fix 0d8d83b6 at trunk — staging verify pending). Returned to IN_PROGRESS; local evidence stands; re-passes on staging verification.