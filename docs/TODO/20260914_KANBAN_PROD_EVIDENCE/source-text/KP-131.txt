RECORD KP-131
SOURCE /Users/frank.nguyen/Documents/silversea/codebase/docs/Kanban-PROD/QA_PASSED/20261109_4-bug1-logout-not-working.docx
KANBAN STATUS QA_PASSED
SHA256 55e9f590d5a46e88e7d89fd62426728097949e37e584b218a24cedb36d145ddb
EMBEDDED IMAGES 12

PART word/document.xml
Please fucking user browser click to test don’t fucknig use headless to test
I still cannot log out after click Dang xuat
This ticket is reopen, if you test plese fucking write list of step you do, attached image evidence, other this ticket cannot be closed
[PREVIOUS CONTEXT]
Nguồn: 20261109_4.docx — BUG 1
Existing: cannot log out
Expect: click Dang xuat can log out
Trạng thái: QA PASSED — chờ deploy prod. REOPEN đã xử lý: bug thật = sheet Tài khoản mobile bị dismiss layer đóng giữa lần nhấn (thiếu data-dropdown-root) → không sinh click. Fix 9ab817ed, deploy staging, verify bằng TAP THẬT 2 vòng.
Tiêu chí nghiệm thu:
1. Click Đăng xuất kết thúc phiên: về màn Đăng nhập (/login), localStorage trống, token cũ bị thu hồi server-side (GET /api/trucks 200 trước → 401 sau khi bấm).
2. Lặp lại được: 2 vai trò (dieuvan - điều độ, giamdoc - giám đốc) đều đăng xuất sạch qua menu người dùng.
[2026-09-13] QA PASSED — đăng xuất HOẠT ĐỘNG trên HEAD (không cần sửa code; báo lỗi tồn tại từ trước khi luồng logout hiện tại landed). Evidence: qa/2026-09-13_20261109_4-bug1-logout/
• Vai trò điều độ (dieuvan): đăng nhập form thật → menu người dùng → bấm Đăng xuất → về /login, localStorage {}; cùng Bearer token gọi GET /api/trucks: 200 trước khi bấm, 401 sau khi bấm — token bị thu hồi server-side thật sự, không chỉ redirect.
• Vai trò giám đốc (giamdoc): cùng luồng → /login sạch.
• Điều hướng /shipments sau khi đăng xuất → bị đẩy về /login (route guard giữ sau khi hết phiên).
• Luồng logout hiện tại: useAuth.tsx (teardown cục bộ ngay + hàng đợi thu hồi token retry khi online); đã có test (useAuth.logout.test.tsx, App.logout-route.test.tsx — xanh trong run 264 files/1623 tests ngày 13/09).
Ảnh minh họa (đính kèm):
Trạng thái: QA PASSED — chờ deploy prod. REOPEN đã xử lý: bug thật = sheet Tài khoản mobile bị dismiss layer đóng giữa lần nhấn (thiếu data-dropdown-root) → không sinh click. Fix 9ab817ed, deploy staging, verify bằng TAP THẬT 2 vòng.
[2026-09-13] REOPEN TEST — browser CÓ giao diện (không headless), bấm chuột thật, 9 bước, ảnh từng bước:
Staging dieuvan: login → Menu người dùng → Đăng xuất → /login, storage rỗng, token 200→401. (step1-3)
Staging LAIXE (dvthuc — vai trò bị báo lỗi): login → /my-trips → menu → Đăng xuất → /login, storage rỗng, token 403→401. (step4-6)
PROD LAIXE (dvthuc): login → /my-trips → menu → Đăng xuất → /login, storage rỗng, /api/auth/me 200→401. (step7-8)
Kết luận: code hiện tại (staging 73508aae / prod d03c684c) đăng xuất ĐÚNG cho cả laixe. Giả thuyết lỗi phía người dùng: tab/app mở từ trước đêm 11/09 còn chạy JS bản cũ (bản treo khi revoke). Đề nghị: Ctrl/Cmd+Shift+R hoặc đóng mở lại tab rồi thử; nếu vẫn lỗi cho biết trình duyệt/thiết bị. Chi tiết đầy đủ: qa/2026-09-13_20261109_4-bug1-logout-reopen/REPORT.md
[2026-09-13] REOPEN → ROOT CAUSE THẬT → FIX → QA PASSED (tap thật, staging 9ab817ed)
Vì sao các lần QA trước sai: toàn bộ dùng click tổng hợp (ref/CDP) — bỏ qua pha NHẤN. Click thật trên điện thoại = pointerdown → mousedown → (nút phải còn sống khi mouseup) → click. Lớp dismiss dropdown toàn cục (useDropdownDismiss) đóng menu khi mousedown ngoài data-dropdown-root; sheet Tài khoản của tài xế không có attribute này → sheet biến mất NGAY LÚC ngón tay nhấn xuống, mouseup rơi xuống trang bên dưới, trình duyệt KHÔNG sinh sự kiện click, logout không bao giờ chạy. Menu dropdown (Menu người dùng) có data-dropdown-root nên vẫn hoạt động — vì vậy QA trước pass, còn bấm thật thì fail.
Các bước đã làm (repro bug trên bản staging CŨ, rồi verify trên bản MỚI):
1. Mở https://vantai.tingting.vip/my-trips trên trình duyệt thật (headed Chrome, viewport điện thoại 390×844) → chuyển hướng /login.
2. Nhập dvthuc / Abc123 bằng form → bấm Đăng nhập → vào /my-trips (Lệnh mới 0 / Đã nhận 2 / Lịch sử 0).
3. Bấm tab 'Tài khoản' trên bottom-nav bằng TỌA ĐỘ thật (mouse move + down + up, không phải click tổng hợp) → sheet Tài khoản mở. [ảnh s8]
4. Nhấn Đăng xuất trong sheet bằng tọa độ thật (195,744) → REPRO: sheet đóng, vẫn ở /my-trips, token còn trong localStorage = đúng hiện tượng người dùng báo. [ảnh s9]
5. Instrument DOM: mousedown trúng nút, mouseup rơi vào phần tử trang ('driver-journey') → không hề có sự kiện click → xác nhận cơ chế gốc rễ.
6. Sửa code: thêm data-dropdown-root cho sheet (Layout.tsx), thêm unit test phát lại cả chuỗi nhấn (test FAIL trên code cũ, PASS trên code mới); full suite 265 files / 1633 tests, tsc sạch, lint 0 error. Commit 9ab817ed.
7. Deploy staging: make demo → buildHash 9ab817ed, asset guard OK.
8. Verify lại trên bản deploy bằng TAP THẬT: login dvthuc → tap 'Tài khoản' (341,815) → sheet mở [ảnh s10] → nhấn Đăng xuất (195,744) → về /login, localStorage RỖNG (không còn token). [ảnh s11]
9. Mở lại /my-trips → bị đẩy về /login (route guard giữ phiên đã chết). [ảnh s12]
10. Lặp vòng 2: login → tap thật cả 2 nút → lại về /login, token rỗng — logout lặp lại được. [ảnh s13]
Ảnh đính kèm (bản quyền trong qa/2026-09-13_logout-staging-driver-recheck/):
s8 — sheet Tài khoản mở, tap thật (bản CŨ)
s9 — REPRO bug: sau tap thật vẫn /my-trips, token còn (bản CŨ)
s10 — sheet mở bằng tap thật (bản MỚI 9ab817ed)
s11 — sau tap thật Đăng xuất: về /login (bản MỚI)
s12 — /my-trips sau logout bị đẩy về /login
s13 — vòng 2 vẫn logout sạch
Gates: unit test press-sequence FAIL trên code cũ → PASS trên code mới (2/2); full frontend 265 files / 1633 tests xanh; tsc 0 lỗi; lint 0 error (73 warning có trước). Deploy staging 9ab817ed (make demo, có rollback snapshot + DB backup). Còn lại: deploy PROD bằng make deploy.