RECORD KP-024
SOURCE /Users/frank.nguyen/Documents/silversea/codebase/docs/Kanban-PROD/QA_PASSED/20260913_3-missing-fk-shipment-children.docx
KANBAN STATUS QA_PASSED
SHA256 0816de4625410f37b46fa069c9a78b8955860e3de2d1d9fdf9e397f757021b9b
EMBEDDED IMAGES 0

PART word/document.xml
Nợ kỹ thuật: thiếu FK shipment_fulfillments/shipment_containers → shipment
Hệ quả thấy 2026-09-13 (tracker 20260912_4 mục 9d): QA cleanup xóa shipment nhưng không xóa con → 154 fulfillment + 10 container mồ côi tích luỹ âm thầm trong dev DB (đã dọn; orphans bị mọi query inner-join che giấu nên không lộ ở UI).Việc cần làm: migration thêm FK shipment_fulfillments.shipment_id → shipments(id) và shipment_containers.shipment_id → shipments(id) (cân nhắc ON DELETE CASCADE hoặc RESTRICT tùy chính sách xóa lô). Trước khi chạy prod: quét orphans trên staging/prod (câu lệnh trong qa/2026-09-13_terminal-guard-and-orphan-cleanup/REPORT.md), dọn nếu có, backup (make demo đã có .db-backups), rồi migrate. Cập nhật test schema nếu drizzle-kit bắt buộc.Nguồn: kanban fix-all wave 2026-09-13.
Trạng thái: QA PASSED — chờ deploy (migration tự chạy ở deploy kế tiếp; staging/prod đã pre-check 0 orphans)
[2026-09-13] QA PASSED — migration 0073 (commit 9c802ac7):
1. FK CASCADE shipment_fulfillments + shipment_containers → shipments: xóa cứng lô hàng kéo theo con, orphan về cấu trúc bất khả — test tích hợp chứng minh cascade thật (3/3).
2. FK RESTRICT trips → shipment_fulfillments: trip đang sống chặn xóa fulfillment dưới nó — test chứng minh lỗi trips_fulfillment_id_fkey đúng.
3. drizzle-kit migrate sạch local (ledger 70); staging + prod pre-check 0 orphans sáng 13/09 — migration tự áp dụng ở deploy kế tiếp, KHÔNG deploy trong run này.
4. Suite điều chỉnh: 2 cleanup xóa trips trước fulfillments; 1 test đổi fixture COMPLETED → DISPATCHED để guard trip-exists còn với tới (guard terminal-lot 9a đứng trước).
Gates: full backend 186 + 1851 = 2037/2037 EXIT=0; tsc 0; lint 0 error; journal invariants 7/7. Bằng chứng: qa/2026-09-13_fk-shipment-children/REPORT.md