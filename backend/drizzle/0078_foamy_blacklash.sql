-- Intentionally empty. Migration 0076 owns debit_note_templates and its
-- billing/customer foreign keys. This generated duplicate is retained as a
-- journal placeholder so existing migration histories remain aligned.
SELECT 1;
